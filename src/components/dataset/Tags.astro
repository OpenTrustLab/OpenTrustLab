---
import '../../styles/tags.css';
export interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---

<div class="tagsContainer">
  {
    tags.map((item) => {
      return (
        <a href="">
          <em class="tagItem">{item}</em>
        </a>
      );
    })
  }
</div>
